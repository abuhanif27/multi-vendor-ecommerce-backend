# Screenshot Placeholders

Place the final production screenshots for the portfolio and README here.

Recommended filenames:

- `developer-portal.png`
- `swagger-ui.png`
- `redoc.png`
- `health-check.png`

The README links to these locations so the documentation layout stays clear even before the production domain is live.